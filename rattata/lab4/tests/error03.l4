//test error
//mixing up . and ->

struct s {
  int f0;
};

typedef struct s str;

int main(){
  int[] A0 = alloc_array(int, 3);
  str*[] A1 = alloc_array(str*, 3);
  for(int i = 0; i < 3; i++) {
    A1[i].f0=A0[i];
  }
  return A1[2]->f0;
}
