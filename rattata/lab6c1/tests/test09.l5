//test return -1
//library functions not tested in test08

int main(){
  string s0 = "what's up buttercup";
  char[] c0 = string_to_chararray(s0);
  string s1 = "what's down my clown";
  if (!string_equal(s0,s1)) return string_compare(s1,s0);
  return 5;
}
